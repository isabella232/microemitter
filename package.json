{
  "name": "microemitter",
  "private": true,
  "version": "0.2.0",
  "main": "build/index.js",
  "scripts": {
    "prepublish": "rm -fr build && mkdir -p build && coffee -p -b index.coffee > build/index.js"
  }
}
