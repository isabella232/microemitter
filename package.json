{
  "name": "microemitter",
  "private": true,
  "version": "0.1.0",
  "description": "an experimental implementation of the oft-botched EventEmitter",
  "main": "index.coffee",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
